# Core (Linux/Colab compatible)
pandas
mplfinance
matplotlib
numpy
google-genai
python-dotenv
Pillow
requests

# Optionnel : indicateurs (fallback inclus dans les bots si absent)
pandas_ta; platform_system != "Windows" and python_version < "3.12"

# Windows only (MetaTrader5 terminal) ; ignoré sur Linux/Colab/Mac
MetaTrader5; platform_system == "Windows"
